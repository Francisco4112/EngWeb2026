extends layout

block content
  h2 Lista de Generos (#{generos.length})
  table.w3-table-all
    tr.w3-teal
      th Id
      th Designacao
      th Numero de filmes
    each g in generos
      tr
        td= g.id
        td= g.nome
        td= g.nFilmes
